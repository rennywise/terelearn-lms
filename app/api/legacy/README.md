# Legacy API routes

Files in `root/` are small bridge files for old root API URLs.

Do not add new endpoint logic here. New code should go in `../endpoints/` and shared bootstrap code should go in `../core/`.
